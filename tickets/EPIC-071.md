# EPIC-071

## Title

Add Git Smart Folder Rebuild Skill

## Goal

Create a reusable `git` Developer Dashboard skill that owns a nested `smart.folder` CLI helper for rebuilding disposable umbrella branches from `origin/master` while replaying numbered child branches in order.

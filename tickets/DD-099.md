# DD-099

## Title

Add the first `git.smart.folder` nested CLI slice.

## Method

TDD

## Acceptance

- `skills/git` contains the required skill-local metadata, docs, changelog, and ticket files
- `skills/git/LICENSE` exists with the MIT license text and `README.md` points to it
- the skill exposes the nested command path `dashboard git.smart.folder`
- the helper rebuilds the umbrella branch from `origin/master`, reapplies numbered child branches in numeric order, refreshes local `SM-*` tags, and never pushes
- local child branches override matching remote child branches
- empty child ranges are skipped with a clear message
- detached `HEAD`, dirty trees, child-branch implicit resolution, base-branch implicit resolution, non-git execution, unknown subcommands, incomplete cherry-pick state, and unsupported conflicts all fail clearly
- Docker tests and 100% statement and subroutine coverage pass
- docs, changelog, commit, and push are completed

## Status

Done

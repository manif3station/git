# DD-100

## Title

Auto-abort disposable smart-folder cherry-picks during rebuild.

## Method

TDD

## Acceptance

- `git.smart.folder update` aborts an in-progress cherry-pick when that state is already on the disposable smart-folder branch being rebuilt
- the rebuild still leaves child branches in place while recreating only the named local smart-folder branch and its local `SM-*` tags
- an in-progress cherry-pick on some other branch is still rejected clearly
- Docker tests pass
- `lib/Git/Smart/Folder.pm` reaches `100.0` statement coverage and `100.0` subroutine coverage
- docs, changelog, commit, and push are completed

## Status

Done
